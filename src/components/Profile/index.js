import React from 'react'

import { ProfileStyle, ProfileHeader, VertText } from '../Styled/Profile'

class Profile extends React.Component {
  render() {
    return (
      <div>
        <ProfileHeader>Stuart James Mitchell</ProfileHeader>
      </div>
    )
  }
}

export default Profile
